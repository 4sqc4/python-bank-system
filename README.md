$readmeText = @"
# Проект: Банковская система на Python (OOP Bank System)

Учебный проект, реализующий систему управления банковскими счетами с использованием принципов объектно-ориентированного программирования. Проект включает в себя иерархию счетов, систему управления клиентами и встроенные механизмы безопасности.

---

## 🛠 Особенности реализации
* **Иерархия счетов:** Использование абстрактных базовых классов (`ABC`) для создания различных типов счетов (Сберегательный, Премиальный, Инвестиционный).
* **Собственная обработка ошибок:** Реализована через кастомные исключения в `exceptions.py`.
* **Безопасность:**
    * Ограничение попыток аутентификации (блокировка после 3 ошибок).
    * Мониторинг подозрительных операций (большие суммы).
    * "Тихий режим" системы (ограничение уведомлений в ночное время).
* **Git Flow:** Разработка велась в отдельных ветках (`feature/task`) с последующим слиянием в `main`.

---

## 📂 Структура проекта
* `abstract_account.py` — Базовый интерфейс для всех типов счетов.
* `bank_account.py` — Стандартный расчетный счет с валидацией валют.
* `saving_account.py` — Сберегательный счет с расчетом месячного дохода.
* `premium_account.py` — Счет с поддержкой овердрафта и увеличенными лимитами.
* `investment_account.py` — Инвестиционный портфель с прогнозированием роста.
* `client.py` — Данные клиента и валидация возраста (18+).
* `bank.py` — Управляющее ядро (логика аутентификации, поиска и управления счетами).
* `main.py` — Консольный интерфейс для взаимодействия с пользователем.

---

## 🚀 Как запустить
1.  **Склонируйте репозиторий:**
    ```bash
    git clone [https://github.com/4sqc4/python-bank-system.git](https://github.com/4sqc4/python-bank-system.git)
    ```
2.  **Запустите приложение:**
    ```bash
    python main.py
    ```

---

## 📈 История разработки (Git)
Проект выполнен поэтапно согласно учебным заданиям:
1.  **Task 1:** Реализация базовой структуры счетов и системы исключений.
2.  **Task 2:** Расширение типов счетов (Saving, Premium, Investment).
3.  **Task 3:** Разработка логики управления банком, клиентами и интерфейса.
"@

# Записываем текст в файл README.md в кодировке UTF8
$readmeText | Out-File -FilePath "README.md" -Encoding utf8

# Добавляем файл в Git и пушим на GitHub
git add README.md
git commit -m "Docs: Add professional README.md"
git push origin main
